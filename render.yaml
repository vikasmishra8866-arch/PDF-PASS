services:
  - type: web
    name: pdf-decryption-engine
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 app:app"
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
